{
	/* <div className="absolute inset-0">
				{[...Array(6)].map((_, i) => (
					<div
						key={i}
						className="absolute w-2 h-2 bg-yellow-300 animate-pulse"
						style={{
							top: `${Math.random() * 20}%`,
							left: `${i * 20 + Math.random() * 10}%`,
							animationDelay: `${i * 0.2}s`,
						}}
					/>
				))}
			</div> */
}
